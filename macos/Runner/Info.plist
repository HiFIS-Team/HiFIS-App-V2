<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<!-- 수출 규정 — 업로드할 때마다 App Store Connect 가 묻는 질문을 미리 답해 둔다.
	     앱이 쓰는 암호화는 HTTPS 뿐이라 면제 대상이다 (자체 암호 알고리즘이 없다).
	     **iOS 에는 처음부터 있었는데 여기만 빠져 있었다** — 그래서 macOS 빌드만
	     `Build is not in an internally testable state` 로 테스터에게 안 내려갔다
	     (2026-08-10 실제로 겪어서 손으로 답해 줬다). iOS 쪽과 같은 값이다. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- Mac App Store 필수. 없으면 업로드가 통째로 거절된다
	     (`90242 ... must contain a LSApplicationCategoryType key` — 실제로 겪었다).
	     iOS 는 안 보는 값이라 빌드 3 까지는 몰랐다. -->
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.business</string>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- 개발 중 로컬 서버(http://localhost:8001)에 붙기 위한 예외.
	     macOS도 iOS와 같이 평문 HTTP를 기본으로 막는다. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
		<key>NSExceptionDomains</key>
		<dict>
			<key>localhost</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
