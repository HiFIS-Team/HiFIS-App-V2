<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="HiFIS"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- FCM 알림 모양 — 안드로이드가 배너를 그릴 때 쓴다.

             **아이콘은 흰 실루엣이어야 한다.** 상태바는 알파만 보고 나머지를
             전부 흰색으로 칠하므로, 색이 든 앱 아이콘을 그대로 쓰면 통짜 네모로
             뭉개진다 (실제로 그렇게 떴다). ic_notification 은 마크의 알파만
             떼어 정사각 캔버스에 담은 것이다.

             안 넣으면 채널도 이름 없는 기본 채널로 잡혀서, 사용자가 설정에서
             알림을 끄고 켤 때 무슨 알림인지 알 수 없다. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/notification_channel_id" />

        <!-- 앱을 보고 있을 때 오는 알림을 직접 그린다 (2026-08-19).

             FCM 은 앱이 **앞에 떠 있으면 배너를 안 띄우고** 이 서비스만 부른다.
             이게 없으면 그동안 온 알림이 어디에도 안 뜬다.
             뒤에 있을 때는 시스템이 띄우고 이 서비스는 안 불려서 겹치지 않는다. -->
        <service
            android:name=".PushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 릴스로 넘길 영상을 내주는 자리 (core/util/reels_share.dart).
             인텐트에 `file://` 을 실으면 안드로이드 7 부터 터진다
             (`FileUriExposedException`) — content:// 로 줘야 한다.

             **권한(`authorities`)을 다른 것과 안 겹치게 둔다** — 플러그인이
             저마다 FileProvider 를 달고 오는데, 같은 이름이면 설치가 통째로
             실패한다(`INSTALL_FAILED_CONFLICTING_PROVIDER`). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.reels"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/reels_paths"/>
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 추첨 영상을 사진 앱에 저장한 뒤 인스타그램을 열어 준다
             (features/work/praise/praise_draw.dart).
             **안드로이드 11(API 30)부터 여기 없으면 안 보인다** — 다른 앱을
             물어보려면 미리 적어 둬야 해서, 안 적으면 깔려 있어도 못 찾는다.
             iOS 의 LSApplicationQueriesSchemes 와 같은 사정이다. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="instagram"/>
        </intent>
        <!-- 릴스 작성 화면으로 바로 보낼 때 인텐트를 받을 앱이 있는지 본다
             (core/util/reels_share.dart). `resolveActivity` 가 여기 없으면
             늘 null 이라 **버튼이 아예 안 뜬다.** -->
        <package android:name="com.instagram.android"/>
    </queries>
</manifest>
